# Sales Pace Command Center Data

Static sanitized aggregate JSON endpoint for the Sales Pace Command Center dashboard.

Files: daily.json, weekly.json, source-map.json, change-log.json.

No raw CRM records, client names, private Slack text, PII, or secrets should be committed here.
